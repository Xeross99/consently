# Pinned for the host application, so `import "consently/banner_controller"`
# resolves without anyone editing their own importmap.
pin "consently/banner_controller", to: "consently/banner_controller.js", preload: true
